"""Create a program that asks the user for a number and then prints out a list of all the divisors of that number.
(If you don’t know what a divisor is, it is a number that divides evenly into another number.
For example, 13 is a divisor of 26 because 26 / 13 has no remainder.)"""

num = int(input("Please write a number here.\n"))
alist = []
x = 0
for x in range(2,100):
    if num % x == 0:
        alist.append(x)
    else:
        continue
print(alist)

"""
Dit was wederom een mooie oefening om een for loop te gebruiken. Ik was even aan het zoeken naar de range maar had dit snel gevonden.
Daarna was het een kwestie van een lege lijst aanmaken en x toevoegen aan de lijst zodra de mod van num gelijk was aan 0.
"""
